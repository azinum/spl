// main.spl

// include "lib/common.spl"

// fn add(a : u64, b : u64) -> u64 + a b;

struct A (
  a : u64,
  b : u64,
  c : u32
);

struct B (
  a : A,
  b : u64
);

fn main -> none {
  print sizeof A;
  print sizeof B;
}
