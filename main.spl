// main.spl

// include "lib/common.spl"

fn add(a : u32, b : u32) -> u32 + a b;

fn main -> none {
  const a : u32 3, 4, 5, 6;
  // print load32 + @a * 3 sizeof u32;
  print add(a, cast load32 + @a sizeof u32 u32);
}
