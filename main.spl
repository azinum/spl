// main.spl

// include "lib/common.spl"

enum u64 A, B, C, D;

fn add(a: u64, b: u64) {
  + a b;
}

fn main -> none {
  let a = "Hello, World!";
  let b = "abcd";
  const c = 12;
  let d = c;
  print d;
}
