// main.spl

// include "lib/common.spl"

fn strlen(any str) -> u64 {
  let length 0;
  while < 0 load8 str {
    = @length + 1 length;
    = @str    + 1 str;
  }
  length;
}

fn main -> none {
  let i 0;
  const count 10;
  if < i count {
    print i;
  }
}
